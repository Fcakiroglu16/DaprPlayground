apiVersion: v1
kind: Service
metadata:
  name: daprone-service-api
  labels:
    app: daprone-service-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: daprone-service-api
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: daprone-service-api
  labels:
    app: daprone-service-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: daprone-service-api
  template:
    metadata:
      labels:
        app: daprone-service-api
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "daprone-service-api"
        dapr.io/app-port: "8080"
        dapr.io/config: "dapr-config"
        dapr.io/log-level: "debug"
    spec:
      containers:
      - name: daprone-service-api
        image: daproneserviceapi:latest
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: ASPNETCORE_URLS
          value: "http://+:8080"
        - name: ASPNETCORE_ENVIRONMENT
          value: "Production"
        imagePullPolicy: Never
